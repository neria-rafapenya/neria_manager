[phases.setup]
aptPkgs = [
  "tesseract-ocr",
  "tesseract-ocr-spa",
  "tesseract-ocr-cat",
  "tesseract-ocr-eng",
  "liblept5",
  "libtesseract5",
  "libarchive13"
]

[phases.build]
cmds = ["mvn -q -DskipTests package"]

[phases.start]
cmd = "java -jar target/*.jar"
